DROP POLICY IF EXISTS "Owner can manage their survey completed companies" ON public.survey_completed_companies; CREATE POLICY "Owner can manage their survey completed companies" ON public.survey_completed_companies FOR ALL TO authenticated USING (owner_id = auth.uid()) WITH CHECK (owner_id = auth.uid()); Then scroll down, add a commit message like 'Add owner RLS policy for survey completed', and commit directly to main.
