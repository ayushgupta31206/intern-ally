DROP POLICY IF EXISTS "Owner can manage their survey completed companies" ON public.survey_completed_companies; CREATE POLICY "Owner can manage their survey completed companies" ON public.survey_completed_companies FOR ALL TO authenticated USING (owner_id = auth.uid()) WITH CHECK (owner_id = auth.uid());
